<Window x:Class="Messaging_Example.InputChildWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputChildWIndow" Height="100" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.ColumnSpan="2" VerticalAlignment="Center">
            Please input some text in the box below
        </TextBlock>
        <TextBox x:Name="TheInput" Text="{Binding UserInput, Mode=TwoWay}"  Grid.Row="1" VerticalAlignment="Center"/>
        <Button Grid.Column="1"  Grid.Row="1" Command="{Binding ReturnInput}">
            OK
        </Button>
    </Grid>
</Window>
